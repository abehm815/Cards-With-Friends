<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Default background -->
    <solid android:color="@android:color/white"/>

    <!-- This stroke color will be tinted dynamically in code -->
    <stroke android:width="4dp" android:color="@color/my_blue"/>

    <corners android:radius="16dp"/>

    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp"/>
</shape>
